CREATE TABLE plants_wishes (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  plant_id INTEGER REFERENCES plants_base(id) NOT NULL,
  user_id INTEGER REFERENCES plants_users(id) NOT NULL
);